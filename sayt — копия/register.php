<?php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
$username = $_POST["username"];
$email = $_POST["email"];
$password = $_POST["password"];

// Проверка введенных данных и добавление пользователя в базу данных

// Перенаправление пользователя на страницу после успешной регистрации или отображение сообщения об ошибке
}
else {
print_r("Регистрация успешна");
}
/ Проверка введенного адреса электронной почты
if (!filter_var($email, FILTER_VALIDATE_EMAIL)) {
echo "Неверный адрес электронной почты";
}

// Проверка сложности пароля
if (strlen($password) < 8) {
echo "Пароль должен содержать не менее 8 символов";
}

// Проверка других требований к данным
// ...
?>